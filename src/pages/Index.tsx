
import React, { useEffect } from 'react';
import { Navigate } from 'react-router-dom';
import Home from './Home';

const Index = () => {
  // This is a simple wrapper that redirects to the Home component
  return <Home />;
};

export default Index;
